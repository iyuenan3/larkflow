# larkflow · 一个租户一份。落到 /srv/larkflow/<租户>/larkflow.env，chmod 600，属主 lf-<租户>。
#
# 这个文件由 **larkflow 自己的解析器**读（`--env-file`），不是 shell、也不是 systemd 的
# EnvironmentFile。规则明确：
#   · `#` 开头整行 = 注释；允许 `export ` 前缀。
#   · 值**整体**被一对引号包住时剥掉那对引号，否则原样保留（含内部引号）。
#   · `$` 与反引号一律是字面量，不展开。
#   · 已存在的环境变量优先（显式 export 是人当场的意图）。
#
# ⚠️ 绝对不要 `source` 这个文件。shell 会剥引号、展开 `$`，把下面的 JSON 和含 `$` 的
#    api_key 悄悄改坏**而且不报错**（这个项目实测踩过）。

# ---- 飞书应用 ----
# lark-cli 里这家客户那个 profile 的名字。为空的话 lark-cli 会连**默认 app**，
# 一台机器跑多家客户时那就是默认路径上的跨租户串号，不报错、不留痕。
# 必填。注释着 = 未设置，doctor 会明确报「没有指定 profile」。
# **不要留占位值**：占位值语法上合法，会被当成真配置用（bootstrap 曾据此建出一个指向
# cli_xxxxxxxxxxxxxxxx 的 profile，而且一声不吭）。宁可缺，不要错。
# LARK_PROFILE=

# 这颗钉子专治「复制单元文件时改漏 profile 名」：写错 profile 名 lark-cli 会 fail loud，
# 写成**另一家的合法 profile 名**不会。doctor 拿它和 profile 真正解析出的 appId 对一遍。
# 值从 `lark-cli --profile <名字> auth status --json` 的 appId 抄。
# LARKFLOW_APP_ID=cli_xxxxxxxxxxxxxxxx

# 卡片回调只有 bot 收得到。配成 user 时按钮点击一条都收不到，且不报错。
LARKFLOW_IDENTITY=bot

# 应用密钥。**可选**：填了 bootstrap.sh 就自动建 profile（少一个手工步骤，重装也不用重配）；
# 不填就自己敲一次 `<租户目录>/lark profile add --app-secret-stdin`。
#
# 关于「写在这里安不安全」，两句实话：
#   · 它**不会更不安全**。lark-cli 在 Linux 上存的是「密文 + 同目录的 master.key」，
#     谁读得到目录谁就解得开，等于明文。这里是真明文。两者都 0600，防线都只有文件权限。
#   · 它也**不能让密钥不落盘**。想做到那个只有一条路：改喂 tenant_access_token
#     （lark-cli 的 external credential 契约认的是 token 不是 secret，实测确认），
#     而那个 2 小时就过期，得由引擎自己去换、自己刷新，会破掉「出入站只走 lark-cli」（ADR-005）。
# 所以填它换来的是省一步，不是多一层保护。别把它当加固。
# LARK_APP_SECRET=

# ---- 引擎 ----
# 必须本地盘：网络盘上 WAL 与 flock 都不可靠，多进程写会丢更新（开不了 WAL 会直接拒绝启动）。
# 不填也行：默认 ~/.larkflow/larkflow.sqlite，而 HOME 已经是本租户专属目录。
# LARKFLOW_DB=/srv/larkflow/<租户>/larkflow.sqlite
LARKFLOW_TEMPLATE=contract
# 交付物落哪个云空间文件夹。不配的话文件落在 bot 的个人空间根目录，真项目里人会找不到。
# LARKFLOW_DRIVE_FOLDER=fldxxxxxxxxxxxx
# 定期对账间隔（秒）。**这不是可选优化**：任务完成事件在真栈上根本不推送（ADR-039 未查明根因），
# 轮询是 task_complete 的主通道。配 0 = 关掉，等于放弃一半入站。
LARKFLOW_SWEEP_SECONDS=120

# ---- 角色映射 ----
# 模板里的 assignee_role → 飞书 open_id。真栈 strict：缺一个就在装配期直接抛，
# 绝不把 `ou_法务` 这种假 open_id 发给飞书（那样报错发生在人工节点挂起那一刻，看不出是角色没配）。
# 整体用单引号包住：内部的双引号才不会被任何解析器当语法。
# LARKFLOW_ROLES='{"负责人":"ou_xxx","法务":"ou_xxx","财务":"ou_xxx"}'

# ---- LLM（ADR-017 多角色路由）----
# 客户会问「我的正文发到哪去」。答案就是这里的 base_url，`larkflow doctor` 会把 host 打出来
# （key 绝不打）。**这是每租户必配项**：客户自带端点是「正文不出他们指定范围」的唯一实现方式，
# 也是主 LLM 配额被一家打满时不连累其他家的唯一办法。
# LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# LLM_API_KEY=在这里填，含 $ 也没关系，本解析器不展开
# LLM_MODEL=doubao-seed-1-6-250615
# 按角色覆盖（模板节点的 model_role 命中谁就用谁，未命中回退上面那组）
# LLM_WRITER_BASE_URL=
# LLM_WRITER_API_KEY=
# LLM_WRITER_MODEL=
# 超时：实测一次真实起草 109.7s，默认 60s 会把它掐断，所以默认值已提到 300。
# LLM_TIMEOUT=300
# LLM_WRITER_TIMEOUT=300
# 备用线路：只写 BACKUP_API_KEY = 同端点换把 key；三项都填 = 换一家。400/422 不切换。
# LLM_WRITER_BACKUP_API_KEY=
