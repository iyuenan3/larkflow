# larkflow 设计草案 · 会变的图 + 交付物流转

> 第二轮讨论产物（两个用户故事驱动）。用 pm-execution `create-prd` 的 8 段式框架写，第 7 段扩成引擎模型。
> **这是讨论草案，不是定案。** 文中「本轮收敛」= 讨论中谈拢的方向，不等于 ADR；真定案要走 `AIREADME/DECISIONS`。本轮（2026-07-24）已与发起人评审通过，待蒸馏进 AIREADME 定案。
> 日期：2026-07-24。状态：立项 pre-code，第一段本地引擎已跑通（commit 1f3ecf4）。

---

## 1. 摘要

larkflow 是飞书原生的工作流引擎，让一个项目的**交付物**（合同 / PRD / 任何文档）在一张**项目进行中可编辑**的图上，被 AI、人、工具接力生产与审核，反复打回重写，直到发起人认可后交付。本草案记录两个真实场景逼出的产品与引擎模型，供评审连贯性、定待决点。

---

## 2. 干系人

| 角色 | 人 | 职责 |
|---|---|---|
| 发起人 / 产品 / 开发 | Maxwell | 定方向、拍架构叉、验能力 |

（当前单人项目，干系人从简。）

---

## 3. 背景

- **立项定位**：飞书原生 AI 工作流编排引擎（LangGraph 驱动）。第一段已跑通一个 8 节点缺陷流的本地引擎：一张固定编排器图**解释** state 里数据驱动的领域 DAG（graph as interpreter）+ SQLite checkpointer + 驱动层。
- **什么变了**：用户提出两个真实场景（合同起草、PRD 细化），暴露出「缺陷流」只是特例。真正的通用形态是「**交付物在一张会变的图上流转**」。这把定位从「跑固定流程」升格为「跑任意会变的交付物流转图」。
- **为何现在可能**：lark-cli 1.0.76 具备完整飞书读写（`docs` / `markdown` / `drive` / `task` / `im`），且飞书文档自带原生版本历史。这让「交付物统一成飞书文档」这条落地路径成立（命门已验，见 7.3）。

---

## 4. 目标

**目标**：让非工程用户在飞书里编排「多人 + 多 AI 协作产出一份交付物」的流程，且**流程图和交付物都能在项目进行中自然演化**。

**价值**：把「发起项目 → 分工 → AI / 人接力 → 审核打回 → 整合 → 交付」的心智，变成飞书里可视、可流转、可追溯的工作流。AI 节点扛收集 / 起草 / 整合的重活，人只在关键处审核，且能随时 bypass 自动放行。

**成功度量（草稿，待细化成 SMART）**：
- 一个真实项目（如一份合同）全程跑通，产出被发起人认可的交付物。
- 至少一次「运行中改图」（加节点 / 加审核）被引擎正确接住。
- 至少一次打回触发**选择性重算**，且未受影响的旁支不重跑。
- 发起人可对某审核节点开 auto-approve，令其自动放行。

**对齐红线**：单一事实源不破（checkpointer 权威、飞书投影，不反向写真相）；入口只走 lark-cli；LLM 走 OpenAI 兼容接口（火山方舟 / 中转站 / 直连，按任务角色路由，各角色独立 key）；clean-room（范式自推，不搬雇主代码 / 命名）。

---

## 5. 市场 / 用户

**为谁**：需要「多人 + AI 协作产出一份重要文档」的项目发起人（产品经理、法务、业务负责人）。

按问题定义（不按人群）：「我要一份需要多方接力、会反复打回的交付物，但不想手动追每个人的进度和版本，也希望 AI 帮我干收集和起草的重活。」

**约束**：
- 用户在飞书生态内工作，交付物以文档为主。
- 用户可能完全不懂技术，引擎的复杂度（图、checkpointer、并发）不能外泄到界面。

---

## 6. 价值主张

**要解决的 job**：编排多方协作、追踪谁在做 / 定稿没、把 AI 接进流程干重活、审核打回不丢版本、流程中途还能改。

**用户得到**：
- 一张能随项目**自己演化**的流程图。
- 交付物自动在节点间**流转**。
- 打回只重算受影响的部分（**省时**），旁支复用旧产出。
- 想省事就 bypass **自动通过**。
- 全程飞书原生：协同、版本历史、评论白捡。

**避免的痛**：手动催进度、版本混乱、打回后全流程重来、AI 与人的产出对不上。

---

## 7. 方案

### 7.1 用户流程（两个故事，简述）

**故事一 · 合同起草（多部门 + 各自产出再整合）**
发起人起项目 → 多个 AI 节点并行收集数据（旧合同 / 内部业务数据 / 外部报价）→ 各部门人工审核数据（拦假消息 / 黑名单供应商）→ 多个 AI 节点并行起草各板块（业务 / 财务 / 法律）→ 各板块人工审核（可 bypass）→ AI 整合节点合成一份合同 → 发起人审核（可打回任一板块重写）→ 可选法务节点整体审 → 交付合同。

**故事二 · PRD 细化（会议驱动 + 同文档协同）**
飞书会议纪要 + PRD 草稿为输入 → 发起人下发给组员 → 组员各写模块（手写 / 用 AI / **自起子项目**，对发起人黑盒）→ 组员在同一飞书文档协同，**定稿后发飞书消息**通知 → 发起人审核（人工，或起个 AI 节点全面审）→ 打回重写 / 再拉会议讨论 → 整体 PRD 评审 → 交付 PRD。

两故事同构：**一个交付物，在一张会变的图上，被 AI / 人 / 工具接力生产与审核，反复打回，直到发起人认可。** 缺陷流是其退化特例（交付物 = 修复，图固定单链）。

### 7.2 核心模型（本轮讨论收敛）

**A. 图模型：冻结线 = 执行前沿的「受控活图」**
- 已完成 / 正在跑的节点冻结；只有未来（还没轮到）的节点可增删改。
- 前沿是一条**线不是一个点**（并行时冻结线 = 所有 done/running 的节点）。
- 打回 = 把前沿往回拉，被拉回的那段节点重回「未来」，于是解冻可编辑。
- 好处：永不改历史，只改未来 → 天然不破单一事实源（已完成节点的产出冻在 checkpointer 里，就是权威）。

**B. 打回：选择性重算 + 运行时多选目标 + 目标解冻**
- 人在审核节点当场手选一组节点 `S` 打回（不是模板预声明的单一 on_fail）。
- 重算集 = `S ∪ 传递下游(S)`；不在集内的旁支**复用旧产出**。
  - 例（`A→{B,C,D}→E(合并)→F→…`）：打回 `{B}` → 只重算 B、E（E 读 B_v2 + C_v1 + D_v1），C/D 不动。
- 打回目标本身也解冻，可先改指令再重跑。

**C. 节点模型：2 role × 3 executor + 配置**
- 维度一 executor：`tool`（确定性程序）/ `llm`（AI）/ `human`（人）。
- 维度二 role：`produce`（往交付物上写）/ `gate`（把关，放行或打回）。
- 「AI 收集」和「AI 起草」是同一类 `(llm, produce)`，差别只在交付物 type 和 prompt = **配置不同**。
- 合并节点（fan-in）不是单独一类，就是个 deps 有多个的 `produce`。
- 业务差异全下沉到配置（交付物 type / prompt / deps / auto 开关 / 会签策略 / model 角色，即 llm 节点路由到哪个模型 / 供应商），引擎永不为「合同 / PRD / 视频」新增节点类型。

| | produce（产出交付物） | gate（放行 / 打回） |
|---|---|---|
| tool | 拉数据 / 调 API / 归档 | 自动化检查（ci_test / 格式校验） |
| llm | AI 收集 / 起草 / 整合 | AI 审核 / 评分 |
| human | 人手写 / 整理 | 人工审核（单人 or 会签） |

**D. 门禁：approval_policy + 打回集 + 意见**
- gate 节点一个配置轴 `approval_policy`：`auto`（自动放行 = bypass）/ `single`（单人拍板）/ `any`（任一人过即过）/ `all`（全员会签）。
- gate 节点产出统一 = `{放行?，若打回→打回哪几个节点，意见}`。
- 审核意见可落成飞书文档评论（`drive +add-comment`）。

**E. 交付物：统一飞书文档 handle，模型 = (容器, region)**
- 交付物 = 一个带 type 的**句柄**（飞书 doc token / 云盘 file token），内容本体在飞书 / 外部，引擎只存指针 + 元数据。
- **对人**是一个飞书文档链接（可看 / 可协同 / 可评论审）；**对下游 AI 节点**，消费时用 `markdown/docs +fetch` 拉正文喂进 prompt。
- 视频 / 二进制（fetch 不出文本）只做**终态交付物**，不进 AI 中间流转。
- 统一产出协议：任何 produce 节点最后一步都「把结果物化到飞书（写 doc / 传云盘），交回 handle」。
- **两种拓扑用一个抽象盖住**：交付物 = `(容器 handle, region)`。
  - `region = whole`：节点 own 整个 doc（各自产出再合并，**故事一**）。用 `markdown`（整篇 overwrite，简单）。
  - `region = section(定位符)`：节点 own 共享 doc 的一段（同文档协同，**故事二**）。用 `docs`（docx，block 级更新 + 飞书原生实时协同，避免并发互相覆盖）。
  - `whole` 是 `section` 的退化，两者是同一模型的两个粒度。选择性重算的复用粒度随之从「整个 doc」推广到「doc 内其他段」，引擎 `stale_downstream` 不变（打回一段只重写那段 + 其下游 gate，兄弟段不动）。
  - 共享协同的约束：doc 要**预划 section**（节点各认领一段写，不自由追加），否则并发 race。符合现实（PRD / 合同本就有模块划分）。

**F. 版本化：靠飞书原生**
- 一节点一个**稳定 doc handle**，重跑 `overwrite` 同一个 doc。
- 选择性重算复用旧产出 = handle 不变、读当前版即可，**不依赖**显式版本机制。
- 「想回看历史版」由飞书原生版本史 + 回滚免费提供（`docs +history-*` / `drive +version-*` / `markdown +diff`），仅作审计。
- 与单一事实源一致：checkpointer 记每次执行是 append-only 权威真相；飞书 doc 是投影（被 overwrite）；飞书 history 是投影侧 bonus，不定真相。

**G. 完成信号：编辑态 ≠ 完成态**
- `human-produce`（人手写文档）：完成靠**显式定稿信号**（发飞书消息 / 完成飞书任务）。引擎**绝不**因「文档不动了」判定稿。
- `human-gate`：完成靠点「通过 / 打回」卡片。
- 两者完成语义不同，配置里分开。

**H. 子项目 spawn**
- 一个节点可展开成一个子 DAG（组员自起子项目：AI 收集 → 审核 → AI 写 → 审核），对上层黑盒，只回交付物。

### 7.3 技术对接（复用 vs 新增）

**seg-1 已有、直接复用**：
- 固定编排器图**解释** state 里的 `dag`（`dag` 是可写 channel、每个 super-step 重读）→ **运行时改图几乎免费**，改 state 就是改图，无需重编译。这是当初为别的目的做的选型，正好接住「活图」。
- `stale_downstream(target)` = 传递下游闭包 → **选择性重算已实现**，用户三个打回例子直接对得上。
- `reopen_resets`（单点执行回边，无并发竞争）、`pending/running/done` 三态、`interrupt`/`resume`、checkpointer 权威。

**要新增**：
- 运行时改图**接口** + 合法变更校验（只在 pending 子图内改，保证仍是 DAG、不删在跑节点）。
- 运行时**多选打回**接口（把 gate 产出从「通过 / 不通过」升级成「通过 / 打回哪几个」）。
- 交付物 **handle 模型**（`(容器, region)`）+ 飞书**写**接口封装（`markdown` / `docs` / `drive` 的 create / overwrite / upload / fetch）。
- `approval_policy`（auto/single/any/all）。
- produce/consume **协议**（产出物化到飞书、消费 fetch 正文）。
- 子 DAG spawn。

**命门已验**：lark-cli 1.0.76 写能力齐全（`markdown +create/+overwrite/+fetch`、`docs +create/+update/+fetch`、`drive +upload/+import`、原生版本史、`+add-comment`、`+member-add`）。产出协议落得了地。

### 7.4 假设与验证状态

- ✅ **已验证（2026-07-24，测试组织实测）**：markdown produce/consume/overwrite/版本 闭环跑通。`+create` 得**稳定 handle**（file_token）+ 可打开 URL；`+fetch` 正文完整回来（下游 AI 可消费）；`+overwrite` 重跑后 **handle 不变、内容与 version 更新**（正是选择性重算复用所需的语义）；飞书原生版本史自动留两版（tag 1/2 + editor），审计白捡、无需自建版本。
- 待验：`drive +upload` 传二进制 / 视频（终态交付物）；`docs +create/+update` 富文档协同（故事二拓扑），含 **docx block_id 跨 update 稳定性**（section 定位符的选择性重算依赖它，性质同已验的 handle 稳定性）；`im` 发审核卡片 + `event` 定稿信号（human 节点）。真写 executor 前先 `lark-cli skills read`，不靠 `--help` 猜 flag。
- 非技术用户能接受「图」这个心智（要验 UX，图的复杂度不能外泄）。
- `interrupt.id` 跨 rehydrate 稳定（seg-1 已核实 LangGraph 1.2.9 成立）。

---

## 8. 发布计划

**v1（第一个真项目跑通）**：
- 单层图（暂不上子项目）+ produce/gate 节点 + 选择性重算打回 + `auto`/`single` 审核。
- **交付物只做独立 doc 拓扑**（`region=whole`，markdown + 每节点一个独立 doc + merge 节点）。这条 produce 闭环已实测。
- 先落一个「各自产出再合并」形态的真实项目端到端跑通。

**v2+**：
- 共享协同拓扑（`region=section`，docx + 预划 section + 子项目产出回填到父 doc 段）。
- 子项目 spawn、会签（`any`/`all`）、AI-gate（AI 审核）。
- 崩溃后对账重建（seg-1 推迟的 finding D）、reopen 预算 / blocked 终态（finding C）。

**本轮已定**：
- 拓扑用 `(容器, region)` 统一模型同时表达两种（见 7.2-E），实现分期（v1 独立 doc / v2 共享协同）。
- 交付物 handle 用 overwrite（已实测：file_token 跨重跑稳定，飞书原生留版本）。

**待决**：
- v1 首个真实项目具体选哪个（独立 doc 拓扑，合同最贴；PRD 偏共享协同、更适合 v2）。
- 共享协同的 docx section 定位符（block_id）跨 update 稳定性需实测（见 7.4 待验）。

---

## 附：与「立项时 5 个架构叉」的对应（供追溯）

| 立项时的叉 | 本轮收敛 |
|---|---|
| A 引入项目 / 交付物层 | **要**。重点是交付物一等公民（handle + 流转），非重型项目状态机。 |
| B 投影粒度（每节点建任务？） | 项目看板级可见 + 定稿信号；不必每节点建飞书任务。 |
| C 门禁（升级五维评分？） | **修正**：win 核心不是五维评分，是「可换执行体 + auto/会签 + 打回流转」。五维评分降为可选增强。 |
| D exec_mode（once / auto） | 收进 `approval_policy` 的 `auto`（bypass）。 |
| E 一致性（胖事件 + 对账） | v2 再做（崩溃对账 = finding D）。 |
