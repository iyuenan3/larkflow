# 凭证绝不入库（红线）
.env
.env.local

# 本地运行态 / checkpointer / 关联表
*.sqlite
*.sqlite-*
*.db

# 跨进程锁（跟着 DB 文件走：<DB>.locks/ 与 <DB>.serve.lock）
*.locks/
*.serve.lock

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
